/// Directory with test samples (e.g. mp3) can be found here.
pub const TEST_SAMPLES_DIR: &str = "src/test/samples";
/// If tests create files, they should be stored here.
pub const TEST_OUT_DIR: &str = "src/test/out";
